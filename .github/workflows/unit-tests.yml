name: Integration Tests
permissions:
  contents: read
on:
  pull_request:
    branches:
      - release-*
      - release/*

  workflow_call:

  workflow_dispatch:

jobs:
  check:
    uses: publishpress/github-workflows/.github/workflows/integration-tests.yml@v3
